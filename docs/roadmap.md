# Статус и что дальше

Сверено с кодом 16 августа 2026. Задание — в [vision.md](vision.md).

## Решения, принятые на старте

| Вопрос | Выбор |
| --- | --- |
| Рынок | Двуязычный сразу (RU + EN) |
| Объём MVP | Всё сразу: чтение, ИИ, планы (аудио исключено 16.08.2026) |
| Аккаунты и синхронизация | Потом, сначала всё локально |
| Стек | Flutter + Riverpod + SQLite, офлайн-first (Drift потом убрали) |

Переводы — только public domain: Синодальный, KJV, WEB. Лицензионные (НРП, СРП
РБО, ESV) подключаются позже через договор или API, парсить с сайтов нельзя.
Данные о людях и местах — TIPNR и OpenBible (CC BY).

## Что сделано

Пайплайн данных ([tool/](../tool/)) собирает офлайн-базу из USFM:
`assets/db/bible.db` — 66,1 МБ, 3 перевода, 93 369 стихов, 4 259 сущностей
(3 142 человека, 1 003 места, 114 понятий), 80 917 спанов упоминаний,
9 506 связей, 4 258 статей, 386 мест с координатами, 341 291 перекрёстная
ссылка. `flutter analyze` чист.

По пунктам задания:

| № | Пункт ТЗ | Статус |
| --- | --- | --- |
| — | Закладки, выделения, заметки («Моё») | Готово |
| — | Параллельные переводы | Готово |
| 1 | Интерактивный текст, тап по сущности без синих ссылок | Готово |
| 2 | ИИ по выделенному фрагменту | UI готов, сервера нет. Параллельные места уже работают офлайн |
| 3 | Система «Кто это?» — bottom sheet | Готово |
| 4 | Карточка персонажа | Частично: статья, связи, упоминания. Нет таймлайна событий |
| 5 | Изображения с оговоркой «внешность неизвестна» | Нет: `image_url` заполнен у 52 из 4 259, в UI не выводится |
| 6 | Галерея персонажа | Нет |
| 7 | Раздел «Исследовать» с фильтрами | Готово: люди/места/понятия, Ветхий/Новый Завет, поиск |
| 8 | Граф отношений с зумом | Готово |
| 9 | Места: карточка + карта + маршруты | Карточка и карта есть, маршрутов нет |
| 10 | Предметы и понятия | Готово: словарь на 3511 статей. 3D-скинии нет |
| 13–15 | Святые, разделение источников, «почему он святой» | Нет |
| 17 | Раздел «История» и хронология | Нет |
| 18 | Связность: один тап раскрывает сеть | Частично: текст, сущности, параллельные места |
| 19 | Шесть типов объектов | 3 из 6: люди, места, понятия. Событий и предметов как отдельных типов нет |
| 11 | Календарь праздников | Готово: даты считаются формулой, выбор традиции, разделение Писания и предания |
| 12 | Выбор конфессии | Готово: переключатель на «Сегодня» |
| 16 | Человек дня | Готово |
| — | Навигация «Главная / Читать / Исследовать / Календарь / Моё» | 4 таба: Читать, Сегодня, Исследовать, Моё. Календарь внутри «Сегодня», отдельной вкладки не будет |

Сверх задания сделано: непрерывная лента глав без перелистывания, поиск с
русской морфологией (беглые гласные, «любовь» находит «любви»), темы чтения,
шеринг отрывка, раздел «Моё» (закладки, выделения четырьмя цветами, заметки).

## Сделано 13 августа, второй заход

- Приложение собрано и запущено на симуляторе и на живом iPhone (release,
  подпись автоматическая, team VPJ76776P6). `flutter run` на устройство падает
  на установке — ставить через `xcrun devicectl device install app`.
- Раздел «Моё» целиком: [lib/data/user_database.dart](../lib/data/user_database.dart),
  [lib/state/user_data.dart](../lib/state/user_data.dart),
  [lib/features/library/](../lib/features/library/). Отдельная `user.db` на том же
  `sqlite3`, что и весь остальной доступ к данным — Drift не понадобился и убран
  из зависимостей. Схема с `uuid`, `updated_at` и мягким удалением: добавлять их
  после того, как у людей появятся заметки, значит мигрировать базу у всех.
- Выделения видны в тексте, у стиха с заметкой стоит метка, тап по ней открывает
  саму заметку.
- Починен переход к стиху: тап по результату поиска раньше только записывал
  позицию и никуда не вёл, потому что лента строится вокруг опоры, посчитанной
  один раз. Теперь это отдельное событие `jumpRequestProvider`, и им же
  пользуются закладки и заметки.
- Иконка приложения из макета Figma: мастер в `assets/icon/app_icon.png`,
  нарезка — `python3 tool/make_icons.py` (15 размеров iOS, 5 плотностей
  Android). В макете иконка была со своими закруглёнными углами и полями:
  поля обрезаны, углы дотянуты соседним фиолетовым, альфа-канала нет.
- **Починены переносы текста.** Синодальный USFM помечает маркером `\m` каждую
  печатную строку бумажного издания — 2615 раз в одном Матфее против 1008
  обычных `\p`, в Послании Петра разорваны даже отдельные слова («Симон» /
  «Петр,» / «раб»). Парсер считал это новым абзацем, и проза шла обрывками.
  Теперь `\m` и `\nb` внутри стиха — продолжение той же строки; `\q1` в
  Псалтири и Притчах по-прежнему разрывает, поэзия не тронута. Затронуто
  1019 стихов. Разделитель между сегментами больше не берётся из случайного
  пробела в конце строки исходника — иначе слова склеивались в «Авраамродил».
- **Починено удвоение слов** («родил НаассонаНаассона»). Вылезло, когда стихи
  перестали рваться: в базе было 10 681 пересекающихся упоминаний — одно слово
  привязано к двум личностям сразу, и рендер выводил его дважды. Сборка теперь
  оставляет самого уверенного кандидата (снято 6181 спан, осталось 80 917),
  а рендер защищён от повторного вывода при любых данных.
- `bibleDbVersion` = 2: у всех, кто уже поставил приложение, копия базы
  заменится сама.
- Первые тесты: [test/user_database_test.dart](../test/user_database_test.dart),
  9 штук. Один сразу поймал нестабильную сортировку «новые сверху» у отметок,
  сделанных в одну миллисекунду.

## Сделано 13 августа, третий заход

- **Параллельные переводы.** Второй перевод выбирается в настройках чтения и
  помнится между запусками. Сопоставление по единому ключу стиха, а не по
  номерам: нумерация у переводов расходится, и по номеру местами показывался бы
  соседний стих. На узком экране пары идут друг под другом (второй перевод — с
  вертикальной линейкой слева), на планшете — в две колонки; порог 560 pt. В
  этом режиме абзацы не собираются: иначе два перевода не выровнять построчно.
  Проверено на iPhone и на iPad.
- **Убран «¶» из KJV** — 2970 стихов начинались с печатного знака абзаца,
  попавшего в текст как символ. Те же места уже помечены маркером `\p`. Символ
  лез не только в чтение, но и в поиск с копированием.

## Сделано 13 августа, четвёртый заход

- **Переход к книге, главе и стиху переделан.** Было: плоский список 66 книг,
  главы раскрывались гармошкой внутри него (список ехал под пальцем, сетка
  Псалтири выталкивала всё остальное), к стиху перейти было нельзя вовсе.
  Стало: три раздельных шага, сетка книг по заветам (весь Новый Завет на одном
  экране), сетка глав, сетка стихов по долгому нажатию на главу, недавние места
  и разбор ссылок — «Ин 3:16», «1 Кор 13», «пс22» ведут прямо к месту.
  Заголовок читалки стал кнопкой: раньше о том, что по нему можно нажать,
  приходилось догадываться.
- **Короткие имена книг** — [lib/core/text/bible_reference.dart](../lib/core/text/bible_reference.dart).
  В Синодальном USFM сокращений нет: и `short`, и `abbrev` там равны полному
  названию, поэтому ссылки выглядели как «Первое послание к Коринфянам 13:4».
  Таблица привычных сокращений живёт в приложении и ключуется кодом книги;
  у английских переводов сокращение по-прежнему берётся из базы. Заодно
  подтянулись подписи в «Моё», в копировании и в шеринге.
- **Оформление по референсу.** У каждой книги своя иконка
  ([lib/features/reader/book_icons.dart](../lib/features/reader/book_icons.dart)):
  в сетке из 39 книг глаз цепляется за форму быстрее, чем читает подпись.
  Берётся системный набор Material — не тянет ни шрифтов, ни сети. Переключатель
  заветов стал сегментированным, с заливкой выбранной половины; рядом кнопка
  порядка: канонический ↔ по алфавиту.
- Тесты разбора ссылок — 11 штук, всего в проекте 27.

## Сделано 13 августа, пятый заход

- **Поиск в пределах книги.** Запрос умел это с самого начала — параметр
  `bookId` был в `search()`, — но попросить об этом было нечем. В поиске
  появилась строка области: «Везде» или «Только Ин», книга выбирается тем же
  экраном, что и переход, в режиме «только книга».
- **Карточка сущности стала навигацией, а не справкой.** Каждое упоминание
  теперь ведёт в текст, и добавлен блок «Где встречается»: книги в каноническом
  порядке со счётчиком, нажатие открывает первое упоминание в книге. Потолок
  списка поднят с 400 до 1000 мест.
- **История переходов.** Прыжок из поиска, закладки или карточки уносит далеко;
  теперь в заголовке читалки появляется стрелка «Назад к Ин 3». Сам возврат в
  историю не пишется, иначе «назад» ходило бы по кругу.
- **Убрана разметка из статей.** `<BR>` в TIPNR написан то строчными, то
  прописными — разбор по точному совпадению пропускал 3368 статей, и тег
  показывался читателю как текст. Заодно снимаются `<strong>`, `<i>`, `<a>`.
  `bibleDbVersion` = 3.
- Короткие ссылки («Ин 3:1» вместо «Евангелие от Иоанна 3:1») дошли до карточки
  сущности и результатов поиска.

## Сделано 16 августа

- **Аудио исключено из планов** по решению Тимура. Зависимость `flutter_tts`
  убрана из pubspec — она ничего не делала и ругалась на каждой сборке iOS.
- **Параллельные места.** 341 291 связь из OpenBible.info (CC BY, производная
  от «Сокровищницы библейских знаний»), 29 319 стихов со ссылками — это 94%
  Писания. Пункт ТЗ «связь с другими местами Библии» выглядел задачей для ИИ,
  но данные лежат в открытом доступе и работают офлайн. Сборка —
  `dart run tool/build_cross_refs.dart`, слой добавляется третьим шагом после
  сущностей. В панели отрывка появилось действие «Параллельные места»: список
  отсортирован по тому, насколько связь подтверждена читателями, диапазоны
  вроде «1Ин 4:9–10» показываются целиком, нажатие ведёт в текст. Тот же слой
  подставляется в запрос к ИИ — модели больше не нужно вспоминать ссылки.
  `bibleDbVersion` = 4, база выросла с 54,8 до 66,1 МБ.

- **Слова оригинала (Стронг).** В базе давно лежали 1 032 755 привязок номеров
  к словам, но что эти номера значат, приложение не знало. Добавлен словарь:
  14 197 статей (openscriptures; текст Стронга 1890/1894 в общественном
  достоянии, оцифровка под CC BY-SA — ссылка стоит в самой карточке).
  В разметке номера записаны с ведущими нулями («H0430»), в словаре — без них,
  из-за чего у 56 323 слов статья не находилась: кладём оба написания, теперь
  без статьи не осталось ни одного номера. `strongs.db` открывается лениво —
  36 МБ копируются только при первом обращении к функции.
  В панели отрывка появилось действие «Слова оригинала».
  Разметка WEB бывает грубой (и «In», и «the» помечены одним ἐν, а «beginning»
  не помечено вовсе), поэтому карточки сгруппированы по слову оригинала —
  иначе одно и то же читается трижды подряд.

## Слова Христа, 16 августа

Функция не работала не из-за кода: в Синодальном USFM маркера `\wj` нет ни
разу, в WEB он стоит 4580 раз. Красить при чтении по-русски было нечего.

Границы переносятся на сборке: 1411 стихов, где речь занимает стих целиком, и
530 — где речь идёт до конца стиха после двоеточия («И сказал им: …»). Речь
посреди стиха (114 случаев в WEB) не переносится: лучше не покрасить, чем
покрасить чужие слова. Итого 1941 стих Синодального перевода. Смещение лежит в
`verses.wj_from`, в настройках чтения написано, откуда оно взялось.

## Оформление, 16 августа

Собран общий набор правил — [lib/core/theme/design.dart](../lib/core/theme/design.dart).
Было восемь разных скруглений, три ширины полей и десяток кеглей: каждый экран
решал за себя. Стало три скругления, одно поле экрана и одна шрифтовая шкала.
Повторявшиеся в четырёх файлах заголовки разделов, чипы и разделители сведены
в общие элементы.

- Нижняя навигация своя, а не системная: у Material под выбранной вкладкой
  рисуется крупная плашка, чужеродная спокойной читалке. Хватает цвета и
  насыщенности подписи.
- Поиск переехал из плавающей кнопки в шапку читалки — на экране чтения не
  должно быть ничего плавающего поверх текста.
- Карточки отделяются рамкой, а не тенью: тень на светлой теме грязнит, а на
  тёмной не видна.
- Проверено на светлой и тёмной палитре. Тёмную до этого дня никто ни разу не
  видел — оказалась в порядке.
- Поймано переполнение в «Моё»: новые чипы шире прежних, и три штуки перестали
  помещаться в строку. Лента фильтров теперь прокручивается.
- Вторым проходом выровнены поиск, статья, панель отрывка, выбор книги и все
  нижние панели. Панель настроек чтения разворачивалась во весь экран и заезжала
  под часы — высота ограничена 85% экрана, ручка снова видна.

## Карта и география, 16 августа

**Лопухина в Викитеке нет.** Я утверждал обратное по результату поиска — там
оказалась страница-заглушка с описанием издания, а сам текст «Толковой Библии»
не выложен. Комментарий остаётся ненайденным: azbyka.ru его держит, но разбор
чужих сайтов у нас запрещён правилами.

**Зато нашлась настоящая ошибка в данных.** У Иерусалима не было координат, а
386 мест с координатами имели по одному упоминанию. Причина: ключи географии —
идентификаторы вида `Jerusalem@Jos.10.1`, а у наших сущностей в идентификаторе
стоит диапазон `Jerusalem@Gen.14.18-Rev`. Совпадали только места с единственной
ссылкой. После привязки по имени мест с координатами стало 1069 вместо 386.

**Карта офлайновая, без тайлов.** Векторная основа — Natural Earth
(общественное достояние): берег, озёра, реки; границы областей — из OpenBible.
Всё обрезано по библейскому миру, упрощено и лежит в базе: 97 линий,
1624 точки, считанные килобайты. Тайлы означали бы чужой сервер и работу только
онлайн, а приложение обещает открываться в самолёте.

Карта показывается в карточке места; точки крупнее у тех мест, о которых чаще
говорит Писание, подпись рисуется только если не наезжает на уже нарисованную.
Нажатие на точку открывает карточку этого места.

## Русские статьи и словарь, 16 августа

«Библейская энциклопедия архимандрита Никифора» (1891) — общественное
достояние, получена через официальный API Викитеки, а не разбором страниц.
4683 статьи, из них сохранено 4581 (остальное — перенаправления и заглушки).

- **1036 человек и мест получили русскую статью** вместо английского перевода
  с TIPNR. В карточке русский текст показывается вместо английского, источник
  подписан.
- **3511 статей не привязались ни к кому** — это предметы, обычаи, понятия и
  имена из неканонических книг. Из них собран «Словарь», четвёртый фильтр в
  «Исследовать». Так закрылся самый слабый пункт ТЗ: скиния, ефод и ковчег
  наконец описаны.
- Библейские ссылки в статьях размечены шаблоном, и при разборе они
  превращаются в обычный текст. Дважды пришлось повозиться: сначала выходило
  «(Исх. Исх 25:9)», потому что название книги уже стояло в тексте перед
  шаблоном, а потом выяснилось, что разделяет их неразрывный пробел, которого
  не ждал разбор.
- Словарь без запроса показывает самые подробные статьи, а не алфавит: тот
  начинается с междометия «А!» и десятка имён из книг Маккавейских.

## Экран «Сегодня», 16 августа

Отдельной вкладки под календарь не будет: сам по себе он пуст 350 дней в году,
а нужен ровно в секунду открытия приложения. Он растворён в новом разделе
«Сегодня» — четвёртая вкладка вместо пятой из ТЗ.

Четыре блока: продолжить чтение (первой строкой — витрина обязана возвращать в
текст одним касанием), праздник дня с отрывком, человек дня, сетка месяца с
точками на праздниках. Переключатель традиции в шапке меняет весь экран:
у православных Успение 28 августа, у католиков 15-го.

Календарь считается на устройстве:
[lib/core/calendar/church_calendar.dart](../lib/core/calendar/church_calendar.dart).
Пасха выводится формулой отдельно по григорианскому и юлианскому счёту, от неё
пляшут подвижные праздники; неподвижные — просто числа. 12 тестов проверяют
даты Пасхи на 2024–2027 в обеих традициях. Праздники, которых нет в Писании
(Успение, Покров, Введение во храм), помечены прямо в тексте карточки — это
первое место, где работает обещание из ТЗ разделять Писание и предание.

Человек дня выбирается детерминированно по дате из 150 самых упоминаемых людей
с русским именем. Сначала пул был 300, и на экран выпал Азаил, сирийский царь
из 4 Царств: формально в базе есть, а человеком дня быть не за что. Подпись —
факты из наших данных («275 упоминаний в Писании · впервые Быт 11:26»), а не
статья: все статьи в базе английские, и на русском экране такая строка читается
как поломка.

Запуск остаётся на «Читать».

## Размер приложения, 16 августа

Было 131 МБ, стало 50,5 МБ. Два независимых решения:

- **Убран KJV.** Два английских перевода в общественном достоянии дублировали
  друг друга; WEB современнее и, в отличие от KJV, размечен номерами Стронга.
  Освободилось 25 МБ: сам текст плюс половина привязок (было 1 032 755, стало
  683 871).
- **Базы едут в бандле сжатыми** и распаковываются при первом запуске:
  50,1 → 16,7 МБ и 24,5 → 8,5 МБ. Последним шагом сборки обязателен
  `dart run tool/pack_db.dart`.

На устройстве раньше занималось около 230 МБ: база лежала и в бандле, и в
рабочей копии, которую приходится делать, потому что SQLite не читает из
бандла. Теперь около 100 МБ, и ещё 25 добавится, если открыть слова оригинала.
`bibleDbVersion` = 5, `strongsDbVersion` = 2.

## Связи и карта, второй заход, 16 августа

Оба места Тимур назвал кривыми, и по делу.

- **Граф связей заменён списками по родству.** Свободный граф с перетаскиванием
  выглядел хорошо на бумаге: у Давида 41 связь, узлы разъезжались за рамку, и
  в окне оставалось пустое поле с одним именем в углу. Теперь родители, супруги,
  братья и сёстры, дети — отдельными группами с числом и чипами имён; ничего не
  нужно двигать пальцем. Один человек попадал в связи дважды (прямая запись и
  обратная), плюс в первоисточнике есть записи без имени вовсе — отсюда пустой
  чип «[ ]» и два одинаковых «Самма». Оставляем по одному чипу на имя,
  безымянные пропускаем.
- **Карта разрежена и приближается.** Было около тысячи точек в окне высотой
  200 pt — сплошная клякса. Показываем места от шести упоминаний плюс само
  выбранное, окно выросло до 260 pt, добавлен зум щипком (до 6×) и подпись, что
  так можно. Подписи рисуем только с русским именем: у части мест выравнивание
  русского соответствия не нашло, и посреди кириллицы висели «Great» и «South».
  Место под подпись выбранного места резервируется первым, иначе соседняя
  ложилась под неё.

Имена на карте местами стоят в косвенном падеже («Иордана», «Ханаанской») —
это форма из самого текста Писания, к которой привязано имя. Чинить это нужно в
данных, не в отрисовке.

## Русские имена и русский текст в списках, 16 августа

Тимур: «в людях какие-то тексты на английском, какие-то на русском вперемешку».
Так и было, причём по цепочке: русское имя сущности искалось только в тексте
Синодального, а русская статья Никифора привязывалась к сущности по этому
имени. Нет имени — нет статьи — в карточке и в списке остаётся английский
перевод с TIPNR.

- **Имя приводится к начальной форме.** В базе лежала та словоформа, которая
  чаще стоит в стихе: «Израиля», «Иудеи», «Ассирию», «Иордана». Заголовки
  энциклопедии Никифора — те же имена в синодальном написании и в
  именительном падеже, и теперь словоформа снимается по ним (1177 имён).
  Правило работает по основе слова, а не по расстоянию правок: рядом в
  словаре стоят «Пасха» и «Пасхор», «Гад» и «Гадорам», и по одной опечатке
  они неразличимы.
- **Имя ищется в словаре, если в тексте его нет.** Синодальный называет
  сущность иначе или пишет со строчной — 200 имён нашлись сравнением
  написаний («Pharaoh» → «Фараон»). Ещё 57 самых заметных вписаны вручную:
  Иисуса Навина Синодальный зовёт просто Иисусом, и слово по частоте
  достаётся Христу; «Gath» и «Геф» не сблизить никаким сравнением букв.
  Итого имён 3964 из 4259 против 3709.
- **Русских статей стало 1796 сущностей вместо 1070** — привязка идёт по
  имени, и выправленные имена вытянули статьи за собой.
- **Английская проза убрана из русского интерфейса.** В списках «Исследовать»
  подпись — начало русской статьи, а где её нет, факты из наших же данных:
  «Ветхий Завет · 41 упоминание». В карточке английская справка Tyndale
  спрятана под «Справка на английском» и раскрывается по нажатию: выдавать её
  за описание нельзя, но и выбрасывать жаль — часто это всё, что о человеке
  известно. Переведены типы мест (30 значений OpenBible) и области.
- **Фильтры в «Исследовать» разведены на два ряда.** Тип (люди, места,
  понятия, словарь) и завет — разные вопросы; в общей ленте завет уезжал за
  правый край. Второй ряд набран текстом, а не чипами, и к словарю не
  показывается вовсе: статьи энциклопедии к месту в Писании не привязаны.

`bibleDbVersion` = 9.

## Бэкенд ИИ, 18–19 августа

Единственный пункт задания, которому нужен сервер, закрыт. Разбор отрывка идёт
не по памяти модели, а по нашим же данным.

- **Сервер** — [server/](../server/), FastAPI:
  `POST /explain` в том формате, который уже ждал клиент, и `GET /health`.
  `sources.py` собирает источники из `bible.db`: статьи Никифора по сущностям
  отрывка, карточки людей и мест, тексты параллельных мест. Подписи вида
  «Ин 3:16» и «1 Кор 13:4–7» разбираются, мусор отсеивается. Ссылки в ответе
  собирает сервер из того, что реально ушло в запрос, — выдуманного источника
  в списке не появится.
- **Ключ модели остаётся на сервере**, в `~/.holybible.env` с правами 600.
  В бандл приложения его класть нельзя: оттуда его достанут.
- **Эндпоинт закрыт общим секретом.** Публичный адрес без него — открытый кран
  на чужой счёт: запросы шлёт кто угодно, платит владелец. Приложение шлёт
  заголовок `x-app-token` (`--dart-define=AI_TOKEN=…`), сервер без него отвечает
  401. Это не защита от разбора бандла, а защита от случайного прохожего.
  Плюс грубое ограничение частоты по адресу.
- **Наружу смотрит Caddy**, не бэкенд. Бэкенд слушает `127.0.0.1:8099` и из
  интернета недоступен в принципе; Caddy держит 443 и проксирует на него.
  Сертификат Let's Encrypt на `holybible-api.eastus.cloudapp.azure.com`
  выпущен и продлевается сам. 80-й порт нужен только для этого.
- **Обе службы переживают перезагрузку**: `holybible-api.service` и
  `caddy.service` в автозапуске.
- Проверено снаружи по-настоящему: `/health` отвечает, без токена 401, полный
  разбор Ин 3:16 с параллельными местами — около 5 секунд, ссылки на месте.

Сборка приложения с подключённым разбором:

```bash
flutter build ios --release \
  --dart-define=AI_ENDPOINT=https://holybible-api.eastus.cloudapp.azure.com
```

Без этого определения приложение собирается по-прежнему и честно пишет, что
разбор недоступен, а вместо него показывает офлайновое: кто и что упомянут в
отрывке и куда ведут параллельные места.

## Доводка до релиза, 19 августа

Общий секрет в бандле был единственным, что по-настоящему мешало публикации.

- **App Attest вместо общего секрета.** Устройство заводит ключ в защищённом
  элементе, Apple его заверяет, сервер проверяет заверение и выдаёт пропуск на
  сутки. Секрет перестал быть общим: красть из бандла нечего, а отозвать
  доступ можно у одного устройства, не трогая остальных.
  [attest.py](../server/attest.py) разбирает заверение по документации Apple,
  [ios/Runner/AppAttest.swift](../ios/Runner/AppAttest.swift) — мост к
  `DCAppAttestService`, [device_attest.dart](../lib/features/ai/device_attest.dart)
  ведёт весь порядок со стороны приложения.
  Заголовок `x-app-token` остался запасным путём для симулятора и на сервере
  по умолчанию закрыт (`ALLOW_APP_TOKEN`).
- **Кеш ответов.** Отрывков, о которых спрашивают, немного, а вопросы к ним
  однотипные: «Ин 3:16 / простыми словами» стоит спросить у модели один раз.
  Повтор отдаётся из кеша мимо модели, мимо квоты и мимо счёта — проверено:
  3,0 секунды против 0,04.
- **Квоты по устройству, а не по адресу.** Прежний счётчик жил в памяти
  процесса и считал по IP: перезапуск обнулял его, а телефоны за общим
  оператором мешали друг другу. Теперь ключ App Attest, 40 запросов в час и
  200 в сутки, значения в переменных окружения.
- **Потолок расходов на сутки.** Упёрлись — сервер честно отвечает, что разбор
  сегодня недоступен, вместо того чтобы молча тратить дальше. Расход в токенах
  считается по ответу модели и виден в `/health`.
- **Сторож живости.** `Restart=always` поднимает упавший процесс, но не
  помогает, когда процесс жив и молчит. `holybible-watch.timer` раз в пять
  минут проверяет ответ и перезапускает службу. Проверено: остановленный
  бэкенд поднялся сам.
- **Отказы читаются по-русски.** Приложение показывает то, что написал сервер
  («слишком много разборов за час»), а не код ошибки.

Чего это не чинит: разбору по-прежнему нужен интернет — модель живёт на
сервере, а не в телефоне. Всё остальное в приложении работает офлайн.

## Абзацы и способ листать, 19 августа

- **В Синодальном не было абзацев вовсе.** Маркер `\p` стоит по одному на
  главу — 28 на всё Евангелие от Матфея, — а `\m` там означает печатную строку
  бумажного издания и рвёт текст посреди фразы («Авраам» / «родил Исаака»).
  Мы правильно считаем `\m` продолжением строки, но из-за этого глава прозы
  шла сплошным полотном. Границы абзацев перенесены из WEB по ключу стиха —
  тем же приёмом, что и слова Христа: перенос идёт только на границе стиха,
  потому что это деление не зависит от порядка слов. Перенесено 10 048
  разрывов, в Синодальном стало 14 500 абзацев против 4452. Поэзию не трогаем:
  в Псалтири свои 2526 строк. `_transferParagraphs` в
  [tool/build_bible_db.dart](../tool/build_bible_db.dart), `bibleDbVersion` = 10.
- **Два способа движения по тексту, на выбор.** Лента остаётся по умолчанию,
  но в настройках чтения появились «Страницы»: глава на экран, следующая —
  свайпом справа налево. Оба режима делят один рендер главы, поэтому
  оформление, выделения и переходы работают одинаково. Выбор помнится между
  запусками.

## Подготовка к App Store, 14 сентября

Приложение выходит от PHARMIQ ACADEMY (`XQ35SJRLQH`): у личной команды
`VPJ76776P6` профили живут семь дней, это бесплатный аккаунт, и публиковать с
него нельзя. Проверка перед релизом нашла баги, которые ревью бы не заметило, а
люди — сразу.

- **Первый запуск мог сломать приложение навсегда.** База распаковывалась прямо
  под рабочим именем. Закрыл приложение посреди распаковки — остался обрезанный
  файл, а раз файл есть, распаковка больше не повторялась. Теперь пишем в
  `.part` и переименовываем в конце.
- **«Задать вопрос» не отправлялся вовсе.** Многострочное поле превращает Return
  в перенос строки, а кнопки не было. Добавлены кнопка и отправка с клавиатуры.
- **Разбор без сети крутил индикатор бесконечно** и показывал сырое
  `ClientException with SocketException…`. У запросов появился предел
  ожидания, отказы объясняются по-русски.
- **Календарь ставил праздники на день раньше** там, где переводят часы:
  западное Вербное воскресенье 2026 года в Берлине выпадало на 28 марта.
  Дни прибавлялись длительностью, а сутки перехода короче 24 часов. В Москве
  часы не переводят, поэтому раньше этого не видели. Тест гоняется и с
  `TZ=Europe/Berlin`.
- **Постраничный режим открывал не ту главу**: при переключении из ленты — ту,
  вокруг которой лента строилась, а при повторном переключении — первую
  страницу прошлого раза. При переходе к стиху страница дёргалась вбок.

Требования App Store:

- **Только iPhone.** Экраны, кроме чтения, на ширину планшета не рассчитаны, а
  заявленную поддержку iPad потом уже не убрать. iPad-версия — отдельным
  обновлением.
- **Согласие перед первым разбором** (правило 5.1.2): текст уходит модели
  OpenAI в Azure. Под ответом — пометка, что его составил ИИ.
- **Без адреса сервера кнопок разбора нет**: вечное «недоступно» — отказ по 2.1.
- Манифест приватности, политика на `/privacy`, сервер больше не пишет
  IP-адреса в журнал (`--no-access-log`), `ITSAppUsesNonExemptEncryption`,
  русская локаль системных виджетов, фон стартового экрана под светлую
  палитру, версия 1.0.0.

## Очередь

1. Войти в Xcode под PHARMIQ ACADEMY, собрать `flutter build ipa`.
2. Сервер: `APPLE_TEAM_ID=XQ35SJRLQH`, выложить `/privacy` с почтой для связи,
   обновить unit-файл.
3. Проверить App Attest на живом iPhone сборкой новой команды, затем
   `ALLOW_APP_TOKEN=0`.
4. App Store Connect: анкета App Privacy по манифесту, скриншоты, TestFlight.
5. Изображения и иконография из Wikimedia.
6. Святые по дням и раздел «История» — внутрь «Сегодня» и «Исследовать».
7. Прочее за фичефлагом, чтобы App Store не отклонил сборку с пустыми
   разделами по правилу 2.1.
8. iPad: раскладки «Сегодня», «Исследовать» и карточек на ширину планшета.

В планах, но не сейчас — по решению Тимура 19 августа:

- Сборка под Android и адаптивная иконка.

Снято с очереди 19 августа, решением Тимура:

- Планы чтения и стрики.
- Маршруты на карте.
- Резервная копия заметок и выделений.

## Долги по гигиене

- Объявлены и не используются: `go_router`, `intl`.
- Поиск идёт в UI-потоке на каждую букву; на двух буквах ввод может
  подтормаживать — нужен debounce.
- Нет CI.
- Сервер живёт на одной VM: её выключение гасит разбор у всех. Остальное
  приложение при этом работает.
